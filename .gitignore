# ============================================================
# Go
# ============================================================
# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of `go build`
/bin/
/dist/

# Go workspace file
go.work.sum

# Dependency vendor directory
vendor/

# Go coverage output
*.out
coverage.html

# Air live-reload temp dir
tmp/

# ============================================================
# Python
# ============================================================
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Distribution / packaging
*.egg
*.egg-info/
dist/
build/
eggs/
parts/
var/
sdist/
develop-eggs/
.installed.cfg
lib/
lib64/

# Virtual environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/
.python-version

# PyInstaller
*.manifest
*.spec

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
pytestdebug.log

# Jupyter Notebook
.ipynb_checkpoints
*.ipynb_checkpoints/

# IPython
profile_default/
ipython_config.py

# mypy / pyright / ruff
.mypy_cache/
.dmypy.json
dmypy.json
.pyright/
.ruff_cache/

# Celery
celerybeat-schedule
celerybeat.pid

# SageMath
*.sage.py

# Environments
.penv

# Spyder project settings
.spyderproject.db
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# Poetry
poetry.lock

# pipenv
Pipfile.lock

# pdm
.pdm.toml

# uv
.uv/
uv.lock

# ============================================================
# React / Node.js
# ============================================================
# Dependencies
node_modules/
.pnp
.pnp.js
.pnp.cjs

# Build output
build/
dist/
out/
.next/
.nuxt/
.cache/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Runtime data
pids/
*.pid
*.seed
*.pid.lock

# Testing
coverage/
.nyc_output/
cypress/videos/
cypress/screenshots/
playwright-report/
test-results/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production
.env.production.local
.env*.local
# Real deploy secrets live only on the droplet — never commit them.
deploy/do/.env.production
**/.env.production

# Terraform — state and tfvars can contain secrets. Keep .terraform.lock.hcl.
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log
*.tfvars
*.auto.tfvars
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
!*.tfvars.example

# Package manager lockfiles (keep one, ignore others as needed)
# yarn.lock
# package-lock.json
# pnpm-lock.yaml

# Parcel cache
.parcel-cache/

# Vite
*.local

# Storybook
storybook-static/

# ============================================================
# OS / Editor
# ============================================================
# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Linux
*~

# VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# JetBrains IDEs
.idea/
*.iml
*.iws
*.ipr

# Vim
*.swp
*.swo
[._]*.s[a-v][a-z]
[._]*.sw[a-p]

# ============================================================
# Secrets / Credentials
# ============================================================
*.pem
*.key
*.p12
*.pfx
*.crt
*.cer
secrets/
.secrets/
credentials/

draft-idea.md

# ============================================================
# single-branch-development hooks bundle (per-worktree, local)
# ============================================================
# Copied hook scripts + env presets live under .github/hooks/.
# track-env.base.sh is the COMMITTED repo-wide preset (shared across every worktree).
# track-env.sh is the LOCAL per-worktree override — never committed.
.github/hooks/track-env.sh
# Local run records + resume breadcrumbs (default RUNS_DIR). Keeping these out of
# the tree also keeps them out of the evidence fingerprint (see references/hooks.md).
runs/
